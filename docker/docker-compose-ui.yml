version: '3.5'
services:
  gc-ui-react:
    container_name: gc-ui-react
    build:
      context: ../ui-react
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      REACT_APP_API_BASE_URL: http://localhost:18080
    stdin_open: true
